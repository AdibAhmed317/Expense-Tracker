import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  radioGroup: {
    display: 'flex',
    justifyContent: 'center',
    maginBottom: '-10px',
  },
  button: {
    marginTop: '20px',
  },
}));
